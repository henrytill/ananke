packages: ./*.cabal

package aeson-pretty
    flags: +lib-only

package semialign
    flags: -semigroupoids

package strict
    flags: -assoc

package these
    flags: -assoc

preferences: mtl >=2.3.1
           , transformers >=0.6.1

allow-newer: *:mtl
           , *:transformers
